# Copilot Instructions

## Project

`handson-j-heatmap` — a hands-on project for building a heatmap visualization.

> **This file is a placeholder.** Update it as the project takes shape.

## Sections to fill in

- **Build, test, lint commands** — e.g., `npm run build`, `npm test`, `npm run lint`, and how to run a single test
- **Architecture** — describe the key components and how they relate
- **Key conventions** — patterns specific to this codebase (data format, component structure, state management, etc.)
